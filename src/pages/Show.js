import { useState } from "react";
import {useParams, useNavigate} from "react-router-dom";

const Show = (props) => {
    return(
        <div className='show'>
            <h1>This is the show pg.</h1>
        </div>
    );
};

export default Show;